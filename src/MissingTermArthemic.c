#include <stdio.h>
#include <stdlib.h>
#include <stddef.h>

int findMissing(const int *nums, size_t n);

int main(){
    return 0;
}

int findMissing(const int *nums, size_t n){
    int *end = nums[n-1];
    int missing = 0;
    int difference = 0;
    for(int *start = nums; start!=end; start++){
        
    }

}